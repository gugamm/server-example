import { describe, it } from 'mocha';
import { expect } from 'chai';

describe('Another example', function () {
  it('Should calculate the sum of 3+2', function () {
    expect(3+2).to.equal(5);
  });
});
